# 给 AI 一双眼睛、一双手,和一个不会说谎的下载器

> 本文是 deepseek-web-search-plugin 的项目文章——讲它为什么存在、如何思考、以及这段时间踩过的坑。工具文档见 [SKILL.md](SKILL.md) 与 [README](README.zh-CN.md)。

## 起点:一个很朴素的不满

大模型什么都好,就是"不知道今天"。问它股价、新闻、最新版本号,它要么坦白说自己不知道,要么更糟——一本正经地编一个。市面上的联网方案大多要 API Key、要付费、要配置,而且只在海外网络下好用。

于是有了这个项目的第一条设计原则:**零 Key、开箱即用、中国大陆网络优先**。必应、搜狗、360、百度,四个免费引擎打主力;浏览器模拟伪装指纹防反爬;纯 HTTP 轻量版做零依赖兜底。没有 Key,没有配额焦虑,克隆下来就能跑。

## 三条铁律

写着写着,这个项目长出了自己的性格。回头看,支撑它的是三条铁律。

### 铁律一:任何单一路径都会失败,所以永远要有下一条

无头浏览器打不开?换纯 HTTP。直链下不了?上 yt-dlp。yt-dlp 被登录墙拦?真实浏览器边播边缓存。浏览器只抓到封面?收割页面 DOM。DOM 是空的?提取正文存文本。

这就是 `--method chain` 的六路兜底链:

```
direct → ytdlp → browser → cache → harvest → text
```

一步失败自动换下一个,首个成功立即返回,每步成败记进 `attempts` 数组。这不是过度设计——每一环都是被真实故障逼出来的。抖音图文帖逼出了 harvest 转路,B 站分离流逼出了分段合并,SPA 懒加载图集逼出了迭代滚动收割。

### 铁律二:不信任一切标称数据

抓包合并出的视频,容器里写着 95 秒,实际解码只有 31 秒——fMP4 的 moov 时长可以随便标。所以这个项目里,**真实时长永远来自 ffmpeg 全程解码实测**,标称超过实测 30% 判"虚标",解码失败判"坏文件"。搜索结果同理:同一个词让三个引擎各搜一份交叉比对,被越多份确认的排越前;单来源、标题党、可疑域名全部打标。宁可少给你一条结果,不给你一条假的。

### 铁律三:绝不造假,如实报告

页面上所有"下载按钮"都跳转应用商店?如实输出 `app_only: true`,告诉用户这个站网页端没有真文件,而不是硬造一个。没装 ffmpeg 无法验证时长?拒绝删除"疑似坏文件"——无法验证不等于损坏。搜不到?说搜不到,给原因,不编来源。这条铁律在 AI 工具泛滥、幻觉横行的今天,可能比任何功能都值钱。

## 视觉模式:给多模态模型的手和眼

2026 年 8 月,DeepSeek 发布了官方视觉模型。这个项目很快长出了 `--method vision`:一个 stdin/stdout JSON 协议驱动的浏览器会话——模型发指令,工具回截图和屏幕状态。

但"看"容易,"做"难。这段时间踩的坑,每一个都值得记一笔:

**坐标点击是赌运气。** 模型从截图猜像素,SPA 页面元素动态挂载,点偏几像素就是点空。解法是 DOM 精准模式:按文字或选择器定位元素、等它可见、取包围盒中心再点、点完验证、失败重试。指令从 `{"action":"click","x":猜的}` 进化成 `{"action":"click","text":"登录"}`。

**无头截图里看不见鼠标。** 系统光标不会被渲染,模型只能靠数字坐标脑内换算。现在会话里注入了一个放大的白色箭头指针,实时跟随——截图里那个大白箭头,就是鼠标现在的位置。

**会话不能替用户死。** AI 脚本崩了,浏览器跟着消失?加了 `--linger`,窗口留给人看完手动关。指令断了两分钟就自杀?看门狗可配置,有人在场自动放宽。"120 秒未检测登录"曾让人困惑半天——后来发现那只是个文案误导的空闲看门狗,它数的是 AI 发指令的间隔,和页面登录毫无关系。现在会话启动第一行日志就自证版本和全部参数,悬案一眼可破。

**必须说在前面:视觉模式(无头浏览器会话)按截图付费,消费金额较大,一般用户不推荐使用。** 普通搜索、下载、抓正文用本工具其他路线全部免费;只有把截图喂给多模态模型这一步产生模型侧费用。真要用,善用 `--max-screenshots` 封顶和 `shot_policy` 降频,能省则省。

## 登录墙:一场打不赢的仗,和不打的智慧

抖音、豆包、DeepSeek 网页版,清一色登录墙。最魔幻的一次:用户扫码,手机上确认成功,网页端纹丝不动——服务端检测到对面是自动化环境,直接拒发登录 session。指纹伪装(playwright-stealth 全套,20 多项深层补丁)也救不了这一层。

硬闯是不行的,但可以绕:**`--login-rescue`**。人肉在自己平时的浏览器里登录目标站(真人扫码,天经地义),工具自动遍历本机 chrome→edge→firefox,把目标域的登录 cookie 接种进会话,再配 `--profile` 持久化用户目录——登录一次,以后无头免登录。

这是整个项目最喜欢的解法类型:不和风控正面冲突,把"人能做的事"和"机器擅长的事"拼接起来。

顺带两条实战里换来的吐槽级警告。其一,别用无头浏览器操控豆包网页版写文档:登录难、动不动弹登录墙、人机验证大概率失败,每试一次还烧一笔 token——文档让 AI 本地写文件不香吗。其二,抖音网页版的登录按钮是个玄学:验证码登录,手机号、收码、填码一路顺畅,最后那个屏幕中央大大的"登录"按钮 AI 死活点不中;讽刺的是,角落里小小的退出按钮反而一点就中。扫码登录则是扫码后无限超时。两条路实测全灭,结论还是那句:人肉登录一次,`--profile` 存住,别跟它耗。

再说豆包:我们最后是登录成功了的——但那是我们,不代表你也能成功。而且就算登录成功、豆包也能正常回话,这个花费还不如你自己在手机上装个豆包 App 划得来。真能用,但真不推荐:省下的那点操作,全变成烧掉的钱了。

## 诚实的已知限制

这个工具全部在中国大陆网络下开发测试,海外未适配。搜狗的 antispider 是 IP/行为层风控,指纹伪装无法通过——如实写进了错误自愈表:换引擎,或养登录态。没有 100% 的反反爬,强风控站仍可能失败;失败时工具会告诉你为什么失败,而不是假装成功。视觉模式(无头浏览器会话)按截图付费,消费金额较大,一般用户不推荐使用。

## 谁写的,谁测的

开发助手:代码由**智谱 5.3**(GLM)编写与维护。测试 AI:视觉会话等能力实测用了**两个多模态模型**——**MiniMax M3** 与 **DeepSeek 多模态模型**(deepseek-v4-flash-vision-exp),对同一路线交叉验证行为与稳定性。

## 写在最后

从一个"让模型知道今天"的搜索脚本,长成一个 5000 行、九个脚本的浏览器工具箱:搜索、下载、校验、视觉操作、登录态管理。它不完美,但每一行防御性代码背后都有一个真实的事故,每一条"已知限制"都是实测得来的诚实。

代码在 GitHub,MIT 协议,欢迎白嫖,欢迎挑刺。作者是一名学生,在课余时间维护它——如果它帮到了你,那这几周和风控斗智斗勇的夜晚就算没白熬。

*2026-08-22*
